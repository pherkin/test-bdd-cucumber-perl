#!/usr/bin/env perl

use strict;
use warnings;
use FindBin::libs;

use Test::BDD::Loader;
use Test::BDD::Harness::TermColor;

my ( $executor, @features ) = Test::BDD::Loader->load( $ARGV[0] );
my $harness  = Test::BDD::Harness::TermColor->new();

$executor->execute( $_, $harness ) for @features;
