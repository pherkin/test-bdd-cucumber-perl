#!/usr/bin/env perl

# See App::pherkin for documentation
use strict;
use warnings;
use FindBin::libs;

use App::pherkin;
my $result = App::pherkin->run( @ARGV );

exit( $result->result eq 'failing' );